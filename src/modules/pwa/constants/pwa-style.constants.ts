export const pwaClasses = {
  updateRegion:
    'fixed start-[max(1rem,env(safe-area-inset-left))] end-[max(1rem,env(safe-area-inset-right))] bottom-[calc(env(safe-area-inset-bottom)+5.5rem)] z-50 mx-auto max-w-md',
} as const;
