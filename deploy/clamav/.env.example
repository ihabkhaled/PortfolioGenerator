# Bind to loopback for local development. Production must use a private VPN or
# private service network; clamd TCP has no authentication or encryption.
CLAMAV_BIND_ADDRESS=127.0.0.1
CLAMAV_BIND_PORT=3310
