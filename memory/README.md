# memory/

Things learned the hard way, written down so they are learned once.

This is not documentation of how the system works — that is `docs/`, `rules/`
and `context/`. This is the set of facts that cost someone an afternoon.

- [gotchas.md](./gotchas.md) — traps in this toolchain, with the symptom that
  identifies each one
- [decisions-log.md](./decisions-log.md) — a dated, append-only record of
  decisions too small for an ADR
