/**
 * Type declarations for the local portfolio-architecture ESLint plugin so the
 * rule test harness (src/tests/unit/eslint-architecture-rules.test.ts) can
 * import it under `allowJs: false`.
 */

import type { ESLint } from 'eslint';

export declare const portfolioArchitecturePlugin: ESLint.Plugin;
